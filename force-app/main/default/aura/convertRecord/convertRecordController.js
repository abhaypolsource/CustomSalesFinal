({
    Init : function(component, event, helper) {
        
        /*var action = component.get("c.getName");
        action.setParams({
            recordId: component.get("v.recordId"),
        });
        action.setCallback(this, function (a) {
            var state = a.getState();
            if (state === "SUCCESS") {
                var storeResponse = a.getReturnValue();

                alert(component.get("v.sourceObjectName"));
                if ($A.util.isEmpty(storeResponse.Company_Name__c)) {
                    //alert('true');
                    component.set('v.AccName', storeResponse.Name);
                }
                else {
                    // alert('false');
                    component.set('v.AccName', storeResponse.Company_Name__c);
                }
                //component.set('v.AccId',storeResponse.Id);
            }
        });
        $A.enqueueAction(action)
        */
    }
})